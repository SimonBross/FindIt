<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:id="@+id/menu_top"
           android:checkableBehavior="single">
        <item
                android:id="@+id/nav_favorites"
                android:icon="@drawable/ic_star_black_24dp"
                android:title="@string/favorites"
                android:checked="true"/>
    </group>

    <group android:id="@+id/menu_middle"
           android:checkableBehavior="single">
        <item
                android:id="@+id/nav_working_student_jobs"
                android:icon="@drawable/ic_laptop_black_24dp"
                android:title="@string/working_student_jobs"/>
        <item
                android:id="@+id/nav_internships"
                android:icon="@drawable/ic_location_city_black_24dp"
                android:title="@string/internships"/>
        <item
                android:id="@+id/nav_thesis"
                android:icon="@drawable/ic_school_black_24dp"
                android:title="@string/thesis"/>
    </group>

    <group android:id="@+id/menu_bottom"
           android:checkableBehavior="none">
        <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings_black_24dp"
                android:title="Einstellungen"/>
    </group>
</menu>
